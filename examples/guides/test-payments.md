---
name: test-payments
description: Run payment retry checks in a fictional Node.js application.
---

# Test payment retries

Inspect the current package scripts before choosing a command. If the project
uses Node's built-in test runner and has the example test file, run:

```sh
node --test test/payment-retries.test.ts
```

This is a guide for the main agent. Sieve does not execute this command.
